<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
     <h1>All Chats !</h1>
     <form action="/chats/new" method="get">
        <button style="background-color: rgb(60, 197, 60) ; color: black; ">New Chat</button>
     </form>
     <br>
     <% for (let chat of chats) {%>
         <div class="chat">
            <p>From : <i><%= chat.from %></i></p>
            <div class="msg">
                <p>Message : <%= chat.msg %></p>
                <form action="/chats/<%= chat._id %>/edit" method="get">
                    <button style="background-color: rgb(60, 197, 60) ; color: black; ">Edit</button>
                </form><br>
                <form action="/chats/<%= chat._id %>?_method=Delete" method="post">
                    <button style="background-color: rgb(60, 197, 60) ; color: black; ">Delete</button>
                </form>
                <br>
            </div>
            <p>Received By : <i><%= chat.to %></i</p>
            <hr>
            <p> <%= chat.created_at.toString().split(" ")[4] %></p> 
            <p> <%= chat.created_at.toString().split(" ").slice(0,4).join("-")%></p> 
         </div>
         <br>
         <br>
         <hr>
        
         <br>
     <% } %>
</body>
</html>

